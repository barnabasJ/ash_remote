# TodoClient

A LiveView client for the `ash_remote` example. It generates standalone Ash
resources from `todo_server`'s published manifest and manages todos over RPC.

See `example/README.md` for how to run the monorepo.
